# Scheduler Handlers Module

Custom handlers for the Scheduler application, packaged as a proper Python library.

## Overview

This module provides handlers and utilities for Scheduler-specific functionality.

## Installation

### For Local Development

```bash
# Clone or navigate to the repository
cd /path/to/extensions/schd

# Install in editable mode
pip install -e package/
```
